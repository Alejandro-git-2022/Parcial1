<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.parcial1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Parcial1"
        tools:targetApi="31">

        <!-- Definición de la actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Definición de la actividad CalcularTotal -->
        <activity
            android:name=".CalcularTotal"
            android:exported="true"> <!-- Cambiado a "false" si no se espera iniciar desde fuera de la app -->
            <!-- Si esta actividad debería ser iniciada desde fuera de la aplicación, mantenlo como "true" -->
            <!-- Si esta actividad NO debería ser iniciada desde fuera de la aplicación, cambia a "false" -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>
